[package]
name = "ppeg-api"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
env_logger = "0.11"
serde = { version = "1.0", features = ["derive"] }
ppeg-core = { path = "../ppeg-core" }
