import { describe, expect, it } from 'vitest'

import { getDagreLayout } from './get-dagre-layout'

describe('getDagreLayout', () => {
  it('should be defined', () => {
    expect(getDagreLayout).toBeDefined()
  })
})
