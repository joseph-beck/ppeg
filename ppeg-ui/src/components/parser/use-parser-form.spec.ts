import { describe, expect, it } from 'vitest'

import { useParserForm } from './use-parser-form'

describe('useParserForm', () => {
  it('should be defined', () => {
    expect(useParserForm).toBeDefined()
  })
})
