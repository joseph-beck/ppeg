import { describe, expect, it } from 'vitest'

import { getElementLayout } from './get-element-layout'

describe('getElementLayout', () => {
  it('should be defined', () => {
    expect(getElementLayout).toBeDefined()
  })
})
